# TODO

- [ ] Add missing dependency `vue-router` to `frontend/app/package.json`.
- [ ] Install dependencies (`npm install`) in `frontend/app`.
- [ ] Restart dev server and verify that Vite no longer fails to resolve `vue-router`.

